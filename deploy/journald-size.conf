# Bound journal size to limit SD-card write wear and keep logs across unclean
# reboots. Persistent storage (/var/log/journal) is retained via Storage=auto.
[Journal]
SystemMaxUse=50M
SystemMaxFiles=10
